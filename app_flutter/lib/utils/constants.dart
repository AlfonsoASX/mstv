import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blueGrey;
  static const accent = Colors.cyan;
  static const success = Colors.green;
  static const warning = Colors.orange;
  static const danger = Colors.redAccent;
}

class AppConstants {
  static const double geofenceRadioMetros = 150;
  static const int intentosMaximosLogin = 3;
  static const Duration duracionSesion = Duration(hours: 8);
}
